{
  "name": "inandi-git-exclude",
  "displayName": "Git Exclude - Local",
  "description": "Add files and folders to your local .git/info/exclude with a single right-click.",
  "version": "3.1.1",
  "preview": true,
  "publisher": "iNandi",
  "icon": "media/logo.png",
  "author": {
    "name": "Gobinda Nandi",
    "email": "gobinda.nandi.public@gmail.com",
    "url": "https://inandi.github.io/portfolio/index.html"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/inandi/git-exclude.git"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:gitExclude.addToExclude"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "gitExclude.addToExclude",
        "title": "$(diff-ignored) Add to .git/exclude",
        "category": "Git Exclude - Local"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "gitExclude.addToExclude",
          "group": "7_modification@100",
          "when": "explorerResourceIsFolder || resourceScheme == file"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "20.x",
    "typescript": "^5.3.0"
  }
}
